# notes/

Drop raw ideas here as `.md` files. Anything you put in this folder becomes a
candidate the agent can turn into LinkedIn / X / dev.to drafts.

No format required. A title line helps:

```
# Lessons from migrating DynamoDB to Postgres

Cut p95 from 800ms to 150ms. The real win was matching the storage engine to
the access pattern, not just "Postgres is better." Worth a post on when NOT to
reach for a key-value store.
```

`README.md` is ignored. Everything else is fair game.
